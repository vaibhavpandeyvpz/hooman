/**
 * Well-known `SessionConfigOption` ids the Hooman ACP agent advertises
 * (see `src/acp/session-config.ts`). Mirrored here as literal string
 * constants since the webview can't import host-side/Node modules.
 */
export const CONFIG_ID_MODE = "mode";
export const CONFIG_ID_MODEL = "model";
export const CONFIG_ID_EFFORT = "effort";
